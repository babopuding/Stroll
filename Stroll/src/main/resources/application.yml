spring:
  profiles: default

datasource:
  map:
      initialize: true
      driver-class-name: "com.mysql.jdbc.Driver"
      url: "jdbc:mysql://localhost/world?verifyServerCertificate?=false&useSSL=false"
      user-name: "root"
      password: "root"
      initial-size: 10
      max-active: 10
      max-idle: 10
      min-idle: 10
      max-wait: 3000
  admin:
      initialize: true
      driver-class-name: "com.mysql.jdbc.Driver"
      url: "jdbc:mysql://localhost/world?verifyServerCertificate?=false&useSSL=false"
      user-name: "root"
      password: "root"
      initial-size: 10
      max-active: 10
      max-idle: 10
      min-idle: 10
      max-wait: 3000
